import { MultilineTextDrawer } from "./MultilineTextDrawer";
export async function loadMultilineTextShape(engine, refresh = true) {
    await engine.addShape("multiline-text", new MultilineTextDrawer(), refresh);
}
